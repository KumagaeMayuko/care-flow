<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha1/css/bootstrap.min.css"
        integrity="sha384-r4NyP46KrjDleawBgD5tp8Y7UzmLA05oM1iAEQ17CSuDqnUK2+k9luXQOfXJCJ4I" crossorigin="anonymous">
    <title>パスワードリセット</title>
    <style>
        body {
        margin: 50vh auto 0;
        transform: translateY(-50%);
        padding: 15px 30px;
        color: #554d4d;
        text-align: center;
        width: 500px;
    }
        .red {
            color: red;
        }
    </style>
</head>
<body>
    <h2>パスワードリセット</h2>
    <form action="show_reset_form.php" method="POST">
        <input type="hidden" name="password_reset_token" value="{{ passwordResetToken }}">
        <!-- Password input -->
        <div class="form-outline mb-4">
            <p>パスワードは8桁以上英数字で入力してください</p>
            <input type="password" name="password" placeholder="* Password" value="{{dataArr.pass}}" id="form2Example2" class="form-control" />
            {%if  err_pass| length > 0 %}<br><span class="red">{{err_pass}}</span>{%endif%}
        </div>

        <!-- Password confirm input -->
        <div class="form-outline mb-4">
            <input type="password" name="password_confirmation" placeholder="* Password Confirm" value="{{dataArr.pass}}" id="form2Example2" class="form-control" />
            {%if err_pass_confirm | length > 0 %}<br><span class="red">{{err_pass_confirm}}</span>{%endif%}
        </div>

        <!-- Submit button -->
        <button type="submit" name="submit" class="btn btn-primary btn-block mb-4">Send</button>

        <!-- Register buttons -->
        <div class="text-center">
            <p><a href="login.php">Sign in?</a></p>
        </div>
    </form>
</body>
</html>
